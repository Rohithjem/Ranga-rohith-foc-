#include <stdio.h>

int main() {
    int start = 1, end = 10;
    printf("Series of numbers from %d to %d:\n", start, end);
    for(int i = start; i <= end; i++) {
        printf("%d ", i);
    }
    return 0;
}
